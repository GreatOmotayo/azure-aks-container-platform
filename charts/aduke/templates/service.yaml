apiVersion: v1
kind: Service
metadata:
  name: aduke
  namespace: {{ .Release.Namespace }}
  labels:
    app: aduke
spec:
  type: ClusterIP
  selector:
    app: aduke
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP