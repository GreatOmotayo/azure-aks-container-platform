replicaCount: 2 # - actual live count is governed by the HPA below once deployed

image:
  repository: "REPLACE_WITH_ACR_LOGIN_SERVER/aduke"
  tag: "latest" # override with a Git SHA in the real CI/CD pipeline
  pullPolicy: IfNotPresent

serviceAccount:
  name: aduke
  azureClientId: "REPLACE_WITH_TERRAFORM_OUTPUT"

service:
  port: 80
  targetPort: 3000 # matches Aduke's PORT default in server.js

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 2 # matches the PDB's minAvailable - never scales below what the disruption budget already assumes
  maxReplicas: 10 
  targetCPUUtilizationPercentage: 70

probes:
  liveness:
    path: /healthz
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3
  readiness:
    path: /ready
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5 
    failureThreshold: 3

ingress:
  enabled: true
  className: nginx
  host: aduke.local 
  path: /

keyVault:
  secretName: aduke-storage-config
  name: "REPLACE_WITH_TERRAFORM_OUTPUT" # module.key_vault.vault_name - the plain vault name
  vaultUri: "REPLACE_WITH_TERRAFORM_OUTPUT" # module.key_vault.vault_uri

azureTenantId: "REPLACE_WITH_TERRAFORM_OUTPUT" # required by the SecretProviderClass parameters

networkPolicy:
  ingressControllerNamespace: ingress-nginx